# Symfony's internal origin. The schedule proxy appends /api automatically.
NUXT_INTERNAL_API_BASE=https://localhost:8000

# Generate with: php bin/console api-key:create "Nuxt frontend" --internal
# Keep the real key in frontend/.env or your deployment secret store.
NUXT_FRONTEND_API_KEY=
