# Symfony runtime mode
APP_ENV=prod

# Application secret used for signing/encryption
APP_SECRET=change-me
API_KEY_PEPPER=change-me
APP_SHARE_DIR=var/share

# Public URLs and CORS allowlist for browser/API access
DEFAULT_URI=https://gridtime.zech.co
CORS_ALLOW_ORIGIN='^https://gridtime\.zech\.co$'
